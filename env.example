# Environment (development or production)
ENVIRONMENT=development

# Database Configuration
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306
DB_NAME=komite_sekolah

# Security - JWT Secret Key (IMPORTANT: change this in production!)
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production

# Server Configuration
SERVER_PORT=8080

# CORS - Allowed Origins
# SECURITY: Always specify exact origins, never use *
# 
# For development: http://localhost:3000 (or your frontend dev server port)
# For production: https://yourdomain.com,https://www.yourdomain.com
# 
# Examples:
# Development: ALLOWED_ORIGINS=http://localhost:3000
# Production: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# 
# Multiple origins: ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
ALLOWED_ORIGINS=http://localhost:3000

